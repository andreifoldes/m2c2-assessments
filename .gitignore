node_modules/
archive/
# CFD-derived face images: not redistributable, deployed out-of-band
# (see README "FNAME-Pairs" section; lists.json stays committed)
assessments/fname-pairs/assets/fname-pairs/images/*.jpg
# Private stimulus-hosting secrets (Netlify site + secret path)
assessments/fname-pairs/tools/netlify.local.env
test-results/
playwright-report/
.playwright-mcp/
temp/
.superpowers/
.claude/

# Local Netlify folder
.netlify
# THINGS item photographs (fair-use research stimuli, not redistributable)
assessments/prices/assets/prices/images/*.jpg
# Netlify secret site config for prices stimuli (never commit)
assessments/prices/tools/netlify.local.env
